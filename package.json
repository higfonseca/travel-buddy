{
  "name": "travel-buddy",
  "version": "1.0.0",
  "description": "Best route finder based on a CSV route file.",
  "main": "./index.js",
  "scripts": {
    "clean": "rm -rf dist node_modules",
    "test": "jest --no-cache",
    "test:coverage": "jest --collectCoverage",
    "tsc": "tsc",
    "build": "npm run clean && npm install && tsc -p .",
    "start:prod": "tsc && node ./dist/app/App.js",
    "cli": "tsc && node ./dist/index.js"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/higfonseca/travel-buddy.git"
  },
  "author": "Higor Fonseca",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/higfonseca/travel-buddy/issues"
  },
  "homepage": "https://github.com/higfonseca/travel-buddy#readme",
  "dependencies": {
    "@types/jest": "^26.0.4",
    "@types/node": "^14.0.19",
    "@types/sinon": "^9.0.4",
    "jest": "^26.1.0",
    "ts-jest": "^26.1.1",
    "typescript": "3.7.5"
  },
  "devDependencies": {}
}
